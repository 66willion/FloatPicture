# FloatPicture 2.2.54

发布日期：2026-04-23

本次版本调整了“随机显示一张”的选图策略，重点如下：

- 保留现有随机入口和“尽量避开当前已显示图片”的规则，只把最后一步选图改为 `shuffle bag`。
- 每一轮会先将当前有效候选图片打乱，再逐张取出，避免在短时间内反复抽到同一批图片。
- 当一轮抽完后会重新洗牌，并尽量避免新一轮的第一张和上一轮的最后一张立刻重复。

发布资产：

- `FloatPicture-2.2.54-signed.apk`
- 本地 release 编译产物
